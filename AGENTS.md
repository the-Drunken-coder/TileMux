# Agent Notes

Simple Elegant solutions are always superior to complex ones.

TileMux is deployed on Cloudflare Workers. Before changing deployment behavior,
read `docs/DEPLOYMENT.md` for the current Worker name, account, bindings,
secrets, GitHub repo, and deploy path.
